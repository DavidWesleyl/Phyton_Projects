#temp = float(input('Informe a temperatura em °C'))
#conversão = (temp * 1.8) + 32 #CONVERSÃ0 EM FAHRENHEIT
#print('A temperatura de {}°C, convertido para fahrenheit, será {:.1f}°F '.format(temp, conversão))

#tempe = float(input('Digite a temperatura'))
#conv = tempe + 273.15
#print('A temperatura de {}°C, convertido para Kelvin, será {}K'.format(tempe, conv))

peso = float(input('Digie seu peso'))
lbs = peso * 2.205
print('Seu peso {}KG, em LBS, será {:.2f}.'.format(peso, lbs))

